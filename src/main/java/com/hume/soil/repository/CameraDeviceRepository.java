package com.hume.soil.repository;

import com.hume.soil.model.CameraDevice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CameraDeviceRepository extends JpaRepository<CameraDevice, String> {
}
